export interface Course {
  id: string;
  title: string;
  description: string;
  imageUrl: string;
  category: string;
}

export interface CourseSegment {
  id: string;
  title: string;
  description: string;
  imageUrl: string;
}
